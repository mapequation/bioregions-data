# Bioregions data

Species occurrence data files used by [Infomap Bioregions](https://github.com/mapequation/bioregions).

Fetched automatically by `npm run fetch-data` (or `npm run build`) in the main repo.
